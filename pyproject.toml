[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "cg-lib",
    "checkpoints",
    "first_out",
    "submissions",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
